# Production build — API lives under /api on the same domain served by nginx,
# so we leave VITE_API_BASE_URL unset (api.js falls back to "/api").
# Uncomment and change only if the API is on a different origin.
# VITE_API_BASE_URL=/api
